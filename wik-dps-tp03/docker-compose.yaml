services:
  my_api:
    build:
      context: .
      dockerfile: Dockerfile

    expose:
      - "3000"

    deploy:
      replicas: 4

  proxy:
    image: nginx:latest

    ports:
      - "8080:80"

    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro

    depends_on:
      - my_api